name: Hello Actions

on:
  push:
    branches: [ main ]
  workflow_dispatch:  # 可以手动触发

jobs:
  hello_world:
    runs-on: ubuntu-latest
    steps:
      - name: 打印信息
        run: echo "🎉 你好 GitHub Actions!"
