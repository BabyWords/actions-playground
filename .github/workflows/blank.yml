name: Hello Actions

on:
  push:
    branches: [ main ]
  workflow_dispatch:  # 可以手动触发

jobs:
  deploy-dev:
    runs-on: ubuntu-latest
    steps:
      - name: 打印信息
        run: echo "deploy dev done!"
  deploy-qa:
    runs-on: ubuntu-latest
    steps:
      - name: 打印信息
        run: echo "deploy qa done"
  deploy-prod:
    runs-on: ubuntu-latest
    steps:
      - name: 打印信息
        run: echo "deploy prod done"
